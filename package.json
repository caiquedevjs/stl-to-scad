{
  "name": "stl-to-bgsd",
  "version": "0.1.0",
  "private": true,
  "description": "Conversor local de STL e 3MF para inserts BGSD/BIT",
  "scripts": {
    "start": "node tools/gui-server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.9.12",
    "jszip": "^3.10.2"
  }
}
